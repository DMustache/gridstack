to check
    v1.1
        GET /_matrix/client/v3/account/whoami +
        ---
        device_id
    v1.2
        GET /_matrix/client/v3/account/whoami +
        ---
        guests
    v1.3
        POST /_matrix/client/v3/register +
        POST /_matrix/client/v3/login +
        ---
        refresh tokens
    v1.7
        GET /_matrix/client/v3/login - untill we not support m.login.token
    v1.17
        POST /_matrix/client/v3/login - untill we not support appservice implementations and usage
        POST /_matrix/client/v3/login - untill we not support appservice implementations and usage
        ---
        appservice implementation

missing
    v1.18
        POST /_matrix/client/v3/register

RFC 8414 OAuth 2.0 Authorization Server Metadata https://datatracker.ietf.org/doc/html/rfc8414
https://en.wikipedia.org/wiki/Augmented_Backus%E2%80%93Naur_form

PCI DSS 4.0 Requirement 8.3.6 for password complexity ^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{12,}$

POST /_matrix/client/v3/register + only m.login.password and user account kind
GET /_matrix/client/v1/auth_metadata +- On implementation to disable Legacy auth should be updated pass
GET /_matrix/client/v3/login +- On 1.18 implementation Oauth2.0 auth should be updated and future m.login.token support, pass
POST /_matrix/client/v3/login +- well_known in View not provided untill implementation not ready. appservice not implemented, well_known not supported
POST /_matrix/client/v3/logout +
GET /_matrix/client/v3/account/whoami +

Pattenrs

Clean Arhitecture


DDD
    Business Invariants Validation

Loose Coupling

Core Principles
     Parse, don't validate

ODR (One Definition Rule).
